akka {
  loggers  = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug
  logger-startup-timeout = 30s
}

service {
  interface = "localhost"
  port      = 8080
  timeout   = 15 seconds
}

nflpickem-referee {
  mysql = {
    host = "127.0.0.1"
    port = 3306
    user = "root"
    password = ""
    database = "pickem"
    pool-size = 5
  }
}